document.addEventListener('DOMContentLoaded', function() {
 document.querySelector("[name='firstname'").addEventListener('keypress',callBack)
 
}) 
function callBack() {
  // select the firstname input 
  // get the value of that input
  // raise an alert with that value
  alert('danger!')
}